name: Build

on:
  push:
    branches: [main]
  pull_request:

permissions:
  contents: read

concurrency:
  group: build-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      # PR number and head SHA are saved in site/_meta/ so deploy.yml can
      # read them. workflow_run payloads don't reliably carry them. deploy.yml
      # removes _meta/ before publishing so it never reaches the live site.
      - name: Build site and save PR metadata
        uses: devcontainers/ci@v0.3
        with:
          runCmd: |
            zensical build --clean
            pr="${{ github.event.pull_request.number }}"
            sha="${{ github.event.pull_request.head.sha }}"
            if [ -n "$pr" ]; then
              mkdir -p site/_meta
              echo "$pr" > site/_meta/pr_number.txt
              echo "$sha" > site/_meta/sha.txt
            fi
      - name: Upload site artifact
        uses: actions/upload-artifact@v4
        with:
          name: site
          path: site
          retention-days: 1
